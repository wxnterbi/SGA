@model IEnumerable<SGA.Web.Models.RegistroAcceso.RegistroAccesoViewModel>

@{
    ViewData["Title"] = "Registros de Acceso";
}

<h2>Registros de Acceso</h2>

<p>
    <a asp-action="Create" class="btn btn-primary">Nuevo Registro</a>
</p>

<table class="table table-bordered table-striped">
    <thead>
        <tr>
            <th>Usuario</th>
            <th>Viaje</th>
            <th>Permitido</th>
            <th>Motivo</th>
            <th>Fecha Hora</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.UsuarioId</td>
                <td>@item.ViajeId</td>
                <td>@item.Permitido</td>
                <td>@item.Motivo</td>
                <td>@item.FechaHora</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-warning btn-sm">Editar</a>
                    <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-info btn-sm">Detalles</a>
                    <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger btn-sm">Eliminar</a>
                </td>
            </tr>
        }
    </tbody>
</table>
